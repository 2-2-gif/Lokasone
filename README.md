# Lokasone

```
git clone --recurse-submodules https://github.com/2-2-gif/Lokasone.git $HOME/repos && chmod ug+x $HOME/repos/toolforge/bin/setup-venvs.sh
```
why exist pub files?
this files is all attemps to success push the last attempt is success